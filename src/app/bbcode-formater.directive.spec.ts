import { BbcodeFormaterDirective } from './bbcode-formater.directive';

describe('BbcodeFormaterDirective', () => {
  it('should create an instance', () => {
    const directive = new BbcodeFormaterDirective();
    expect(directive).toBeTruthy();
  });
});
